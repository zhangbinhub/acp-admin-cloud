spring:
  profiles:
    active: prod
  application:
    name: server-eureka
info:
  app:
    name: ${spring.application.name}
    version: 1.0.0
  build:
    author: zhangbin
    active: ${spring.profiles.active}